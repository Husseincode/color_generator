/** @format */

export interface RGBTypes {
  red: number;
  green: number;
  blue: number;
}

export interface RGBATypes {
  red: number;
  green: number;
  blue: number;
  alpha: number;
}

export interface HSLTypes {
  hue: number;
  saturation: number;
  lightness: number;
}

export interface HSLATypes {
  hue: number;
  saturation: number;
  lightness: number;
  alpha: number;
}

export interface HEXTypes {
  hex: string;
}
